# Argus — Competitive Intelligence Monitor
# Core dependencies

# OpenAI SDK — LLM summarization via gpt-4o
openai>=1.30.0

# HTTP client for scraping
requests>=2.31.0

# HTML parsing + boilerplate stripping
beautifulsoup4>=4.12.0
lxml>=5.1.0          # faster parser backend for BeautifulSoup

# Scheduling — APScheduler drives the daily cron loop
APScheduler>=3.10.0

# Config and env
python-dotenv>=1.0.0
PyYAML>=6.0.1

# Optional — only needed when email.provider = "sendgrid"
# sendgrid>=6.11.0
